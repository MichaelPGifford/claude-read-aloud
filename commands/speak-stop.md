---
description: Stop reading aloud
---

Run exactly this one bash command, then reply with only "🔇 stopped":

```
python3 "${CLAUDE_PLUGIN_ROOT}/scripts/speak.py" --stop
```

If `python3` is not found, retry the same command with `python`.
